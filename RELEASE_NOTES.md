## 🚀 v1.5.8 更新说明 (Release Notes)

### 📺 媒体连续性检测、单频道自动自愈与试播恢复增强

本版本针对 Jellyfin 与 Web 管理面板试播时偶发的瞬间卡顿、声音卡顿后变为异常低沉，以及刷新页面仍无法恢复的问题，完善虎牙 HLS 时间线连续性检测、FFmpeg 异常识别、单频道自动自愈和故障证据保留，并升级管理面板试播播放器的深度恢复能力。现有配置、M3U 地址和播放令牌保持兼容，本版本不启用音频转码。

#### 核心变更

1. **🐯 增加虎牙 HLS 时间线连续性保护**
   - 同一频道的虎牙播放列表拉取、比较与缓存改为串行处理，避免并发响应乱序造成旧清单覆盖新清单；
   - 综合比较 `EXT-X-MEDIA-SEQUENCE`、分片身份和上下游断点，识别媒体序号跳跃、回退及无重叠时间线；
   - 比较分片时忽略 CDN 域名与动态签名参数变化，避免正常的域名切换或签名刷新被误判为断流；
   - 确认真实时间线断裂时自动补充 `EXT-X-DISCONTINUITY`，帮助 Jellyfin、hls.js 等播放器安全建立新的音视频时间线；
   - 严格相邻但播放窗口恰好不重叠的清单仍判定为连续，不会插入虚假断点或触发无意义重启。

2. **🔄 新增按频道隔离的媒体异常自动自愈**
   - 从 FFmpeg 日志中识别大幅时间戳跳变、跳过分片和 `Non-monotonic DTS` 等连续性异常；
   - 使用启动保护期、重复次数和组合证据区分正常时间戳校准与真实媒体故障，降低误重启概率；
   - 确认异常后只重启受影响的频道，并刷新对应的虎牙提取器和播放列表缓存，不影响其它频道播放；
   - 每条故障事件均绑定 FFmpeg 会话 ID 与进程 PID，旧会话残留日志不会误杀已经启动的新进程；
   - 增加 90 秒恢复冷却及 10 分钟内最多 3 次自动恢复限制，冷却期间保留待恢复状态，避免重启风暴。

3. **🧾 完善 FFmpeg 日志轮转与故障现场保留**
   - 新推流会话启动时保留上一份 FFmpeg 日志，不再直接覆盖可能包含关键异常的故障记录；
   - 即使日志达到大小上限或临时写盘失败，仍持续排空并分析 stderr，避免日志管道阻塞 FFmpeg；
   - 自动恢复前保存结构化事件、故障前后虎牙清单、本地输出清单、近期 stderr 及最后若干 TS 分片；
   - 诊断快照按单次、单频道和全局容量进行限制，并自动清理旧证据，避免长期运行占满 HLS 临时目录；
   - 关键恢复过程同步输出精简生命周期日志，方便通过 Docker 日志判断异常类别、冷却状态和恢复结果。

4. **🖥️ 升级 Web 试播播放器与深度恢复机制**
   - hls.js 从 `1.5.8` 升级并固定为 `1.7.1`，改进新版浏览器中的 HLS、MediaSource 与音视频错误处理；
   - 新增相邻分片 PTS 漂移、音频编码变化、AAC/采样率/时间戳异常及 SourceBuffer 错误检测；
   - 普通网络故障优先执行轻量重连，持续卡顿或媒体解码异常时完整销毁并重建 Hls、MediaSource 和视频元素状态；
   - 只有视频实际播放进度才视为恢复成功，后台继续下载分片但播放已经停滞时不再错误清零恢复计数；
   - 增加分级恢复次数、重建冷却和旧播放器事件隔离，避免无限恢复循环及销毁后的旧回调干扰新会话。

5. **📊 扩展媒体连续性健康检查与运行指标**
   - 管理员认证后的 `/api/metrics` 新增每频道连续性信号、确认异常、自动恢复次数及当前恢复状态；
   - 公开的 `/api/health` 与 `/api/ready` 增加不含频道 ID、播放地址和令牌的 `mediaContinuity` 汇总；
   - 当前存在未恢复、等待恢复或正在恢复的媒体异常时返回 HTTP 503，恢复完成后自动回到正常状态；
   - 历史异常和恢复次数只用于观察，不会让服务永久处于不健康状态；
   - 保留现有健康接口字段和数据类型，兼容已有 Docker 健康检查及外部监控配置。

#### 升级提示

- **现有用户**：可直接升级，频道配置、Cookie、推流模式及其它现有设置保持不变；
- **Jellyfin / IPTV 用户**：现有 M3U 订阅地址、6 位播放令牌、旧长令牌及兼容路径均不需要更换；
- **音频处理**：本版本不启用音频转码，继续保持原有媒体处理方式，不增加额外 CPU 占用；
- **故障排查**：发生自动恢复后，可在对应频道的 `hls_stream/<频道>/diagnostics/` 中查看最近诊断快照；该目录位于 HLS 临时存储时，容器重新创建后不会永久保留；
- **Docker 用户**：媒体连续性故障期间容器可能短暂显示为 `unhealthy`，程序会优先执行单频道自愈，不会因此主动重启整个容器。

---

## 🚀 v1.5.7 更新说明 (Release Notes)

### 🔢 6 位短令牌与更易输入的 M3U 地址

本版本重点改善电视盒子、投影仪和 IPTV 终端上手动输入订阅地址的体验：新播放令牌改为纯 6 位数字，标准订阅地址缩短为 `/p/{token}.m3u`，同时增加错误重试锁定和可选的令牌鉴权开关。网页管理登录与 Jellyfin / IPTV 播放仍完全解耦。

#### 核心变更

1. **🔢 新生成令牌改为 6 位纯数字**
   - 首次创建管理员密码或主动轮换时，使用系统加密随机数生成 `000000`～`999999` 的固定 6 位令牌；
   - 令牌不包含字母、横线、下划线或其它连接符，便于通过遥控器或虚拟键盘输入；
   - 轮换时保证新令牌与当前令牌不同；
   - 升级前已生成的长令牌继续有效，只有主动轮换后才替换为 6 位数字，避免升级导致现有客户端突然断流。

2. **⌨️ 新增易输入的短 M3U 订阅地址**
   - 管理面板、首次设置响应与令牌轮换响应统一显示 `http://<ip>:9898/p/<6位令牌>.m3u`；
   - M3U 内的 HLS 清单和相对 TS 分片仍自动继承同一播放令牌，用户无需手工输入频道内部地址；
   - 原 `/p/{token}/jellyfin.m3u` 路径继续兼容，已经配置在 Jellyfin / IPTV 客户端中的旧地址无需立即更换。

3. **🛡️ 增加播放令牌防暴力枚举锁定**
   - 按服务端看到的连接来源分别统计错误令牌尝试；
   - 同一来源在 10 分钟窗口内前 4 次错误保持返回 `404`，第 5 次开始锁定 10 分钟并返回 `429 + Retry-After`；
   - 限速同时覆盖 M3U、HLS 清单与 TS 分片路径，正确播放请求不进入失败计数；
   - 验证成功、轮换令牌或切换鉴权开关后，会清理相关失败记录。

4. **🏠 新增“关闭播放令牌鉴权”开关**
   - 播放令牌弹窗中新增鉴权开关，默认启用；旧 `config.json` 缺少该字段时也会安全地默认为启用；
   - 关闭前显示明确风险确认，关闭后改用 `/jellyfin.m3u`、`/live/*` 无令牌地址；
   - 重新启用后立即恢复带令牌路径，开关状态持久化到 `PlaybackTokenAuthEnabled`；
   - 关闭鉴权只适用于完全不对公网开放的可信局域网环境。

5. **🎨 同步优化界面、缓存与部署文档**
   - 令牌弹窗增加开关状态、安全提示、短地址复制与 6 位令牌轮换文案；
   - 更新静态资源缓存标识，避免升级后继续加载旧脚本或样式；
   - README 补充默认短地址、旧路径兼容、鉴权开关、防暴力枚举和客户端配置示例。

#### 升级提示

- **现有 Jellyfin / IPTV 用户**：原长令牌和 `/p/{token}/jellyfin.m3u` 地址继续有效，可直接升级；
- **需要短地址的用户**：升级后登录面板，执行一次“轮换 6 位数字令牌”，再将新的 `/p/123456.m3u` 形式地址更新到客户端；
- **可信局域网用户**：如确认服务端口完全不对公网开放，可在播放令牌弹窗关闭鉴权并使用 `/jellyfin.m3u`；
- **公网或反向代理用户**：请保持令牌鉴权启用，并继续配合 HTTPS、防火墙或反向代理访问控制使用。

---

## 🚀 v1.5.6 更新说明 (Release Notes)

### ✨ 首次访问直接创建管理员密码

本版本重点改善首次部署及从无密码版本升级时的管理端体验：用户打开 Web 面板后即可直接创建管理员密码，无需查找日志、复制初始化码或手工调用 API。已经设置密码的实例保持原有登录方式，现有配置和播放地址不受影响。

#### 核心变更

1. **🧭 新增 Web 首次使用引导**
   - 检测到尚未配置管理员密码时，登录页自动切换为“首次使用 · 创建管理员密码”界面；
   - 页面只需填写新密码与确认密码，设置成功后自动建立管理会话并进入控制台；
   - 已经设置过密码的实例仍显示普通登录界面，不会重复触发初始化流程；
   - 从没有管理员密码的旧版本升级时保留频道、Cookie、推流模式等现有配置，无需重建配置文件。

2. **🧩 统一所有部署方式的初始化流程**
   - `localhost`、局域网 IP、Docker、NAS 与反向代理访问均使用同一套面板引导；
   - 取消首次设置验证码，不再要求用户查看容器日志或在命令行中完成初始化；
   - 项目没有默认管理员密码，也不会把 SSH、系统 `root` 密码或直播平台 Cookie 用作后台凭据。

3. **🔐 保留必要的认证与接口防护**
   - 初始化接口只在尚未设置管理员密码时开放，首次成功后再次调用会被拒绝；
   - 保留专用请求头、JSON 格式、请求体大小及密码长度校验，降低普通跨站表单误触风险；
   - 管理员密码继续使用 PBKDF2 加盐存储，管理会话使用 `HttpOnly`、`SameSite` Cookie，普通登录仍有失败限速保护；
   - 公网部署建议首次启动后立即完成设置，并配合防火墙、访问控制及 HTTPS 反向代理使用。

4. **🔑 保持管理登录与 Jellyfin 播放完全解耦**
   - 首次设置密码时同步生成 256 位随机播放令牌，M3U、HLS 与分片继续使用 `/p/{token}/*` 受保护路径；
   - Jellyfin / IPTV 拉取播放地址不依赖网页登录 Cookie，管理员退出或 12 小时会话过期不会造成断流；
   - 已配置密码和播放令牌的现有实例不会自动轮换令牌，原 M3U 地址保持有效；
   - 只有从无播放令牌的旧版本升级，或主动执行播放令牌轮换时，才需要在客户端更新订阅地址。

5. **🎨 简化初始化界面与交互**
   - 初始化页面仅保留新密码、确认密码两个深色主题输入控件，并提供显示/隐藏密码按钮；
   - 修复输入框宽高、间距与卡片高度不协调的问题，在桌面和较矮视窗中均可完整操作；
   - 保留键盘导航、无障碍名称、实时一致性校验、错误提示与提交中状态；
   - 更新静态资源缓存标识，避免访问过旧版 `v1.5.6` 页面后继续加载已缓存的脚本与样式。

6. **📚 完善 README 部署与升级说明**
   - Docker Compose、Docker CLI、NAS 和 Windows 原生部署步骤统一改为面板首次设置流程；
   - 补充旧版本升级行为、修改密码、忘记密码与公网部署安全提示；
   - Windows ZIP 继续由 GitHub Actions 云端构建并附加到 Release，本地不额外制作或上传压缩包。

#### 升级提示

- **已配置管理员密码的用户**：直接升级即可，原密码、管理配置与播放令牌保持不变；
- **从无密码版本升级的用户**：升级并启动后直接访问面板，按引导创建密码，不需要日志验证码或初始化命令；
- **Jellyfin / IPTV 用户**：网页登录状态不会影响远程 M3U 拉取。若本次首次生成了播放令牌，请把控制台显示的新 M3U 地址同步到客户端；
- **公网部署用户**：取消首次验证码后，未初始化实例在可访问期间允许首位访问者创建管理员密码，请在启动后立即设置，或先用防火墙/反向代理限制访问来源。

---

## 🚀 v1.5.5 更新说明 (Release Notes)

### ⚙️ 发布流水线全面升级至 Node.js 24

本版本是一次发布基础设施维护升级，将 Windows Release 与多架构 Docker 镜像流水线使用的 GitHub Actions 全面迁移至 Node.js 24 兼容版本，消除 Node.js 20 生命周期结束警告，同时保持应用运行逻辑和现有发布产物不变。

#### 核心变更

1. **🟢 升级 GitHub 官方基础 Action**
   - `actions/checkout` 从 `v4` 升级至 `v7`，Windows 与 Docker 两个任务统一使用 Node.js 24 运行时；
   - `actions/setup-dotnet` 从 `v4` 升级至 `v6`，继续安装和构建 .NET 10；
   - `softprops/action-gh-release` 从 `v2` 升级至 `v3`，保留现有 Release 标题、emoji 更新说明和资产上传方式。

2. **🐳 升级 Docker 多架构发布链路**
   - `docker/setup-qemu-action` 与 `docker/setup-buildx-action` 统一升级至 `v4`；
   - `docker/login-action` 升级至 `v4`，继续使用 GitHub Actions 临时令牌登录 GHCR；
   - `docker/metadata-action` 升级至 `v6`，保留语义化版本号与 `latest` 标签；
   - `docker/build-push-action` 升级至 `v7`，继续同时发布 `linux/amd64` 和 `linux/arm64` 镜像。

3. **🛡️ 移除 Node.js 20 兼容债务**
   - 所有目标 Action 的官方 `action.yml` 均已确认声明 `runs.using: node24`；
   - 不使用临时且不安全的 `ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION` 兼容开关；
   - 避免 GitHub Runner 在移除 Node.js 20 后导致后续版本无法自动发布。

4. **📦 保持发布产物与触发规则不变**
   - 继续由 `v*` Tag 自动触发 Windows x64 自包含单文件打包、FFmpeg 集成和 GitHub Release 创建；
   - Windows ZIP 仍由 GitHub Actions 云端生成，本地不额外制作或上传压缩包；
   - GHCR 的版本标签与 `latest` 仍指向同一多架构 OCI 镜像索引。

5. **✅ 保持应用行为完全兼容**
   - 本次不修改管理端登录、播放令牌、M3U/HLS 地址、频道配置或 FFmpeg 推流逻辑；
   - 现有配置文件、Jellyfin / IPTV 订阅地址及独立播放登录态不受影响；
   - 已核对新 Action 的全部现用输入参数，并通过 Release 构建、实际 Actions 发布与 GHCR 平台验收。

---

## 🚀 v1.5.4 更新说明 (Release Notes)

### 🎨 管理端输入控件与登录体验优化

本版本集中修复 Web 管理端密码输入框使用浏览器默认白底、不同表单控件高度不一致，以及嵌套弹窗重复缩进导致输入区域过窄的问题，并对登录页的视觉、交互与无障碍体验进行统一优化。

#### 核心变更

1. **🔐 重构登录密码输入控件**
   - 移除输入框上方重复的“管理员密码”可见标签，由占位文字直接提示输入内容；
   - 保留 `aria-label` 无障碍名称，键盘导航和读屏软件仍可准确识别密码字段；
   - 新增左侧锁图标、右侧显示/隐藏密码按钮，以及对应的悬停、聚焦和按压反馈；
   - 退出登录或会话失效时自动清空密码并恢复隐藏状态，避免下次登录暴露输入内容。

2. **🎨 修复密码框白底与浏览器自动填充样式**
   - 登录密码框改为与深色控制台一致的渐变背景、边框、圆角和蓝色聚焦光晕；
   - 修复 `password` 类型未继承全局表单样式而回退为浏览器默认白底的问题；
   - 对 Chromium 自动填充状态设置深色背景、文字和光标颜色，保存过的密码不再破坏页面主题。

3. **📐 统一全站表单控件尺寸**
   - 文本、密码、URL、邮箱、电话、搜索、数字输入与下拉框统一为 `100%` 宽度和 `40px` 高度；
   - 登录密码框单独采用更舒适的 `50px` 高度，登录按钮同步调整为 `48px`；
   - 统一占位文字、内边距、字体、边框和焦点状态，同时保持复选框、频道开关及隐藏字段不受影响。

4. **🧩 修复播放令牌与密码弹窗对齐**
   - 消除 `.modal-body` 与嵌套 `.form-group` 重复左右内边距，让输入框与状态条、警告条使用相同边界；
   - M3U 地址框与复制按钮高度对齐，长地址自动省略显示，窄屏下自动改为纵向排列；
   - 修改管理员密码弹窗中的当前密码、新密码、确认密码恢复为统一深色、完整宽度和一致高度；
   - 同步修复主机地址、播放令牌轮换及频道编辑等表单的宽度和网格适配。

5. **♿ 优化错误提示与交互验收**
   - 登录错误为空时不再预留多余空白，发生错误时显示紧凑、清晰的红色提示块；
   - 密码显示状态同步更新按钮名称和 `aria-pressed`，保证视觉与辅助技术状态一致；
   - 已通过真实浏览器验证登录、密码切换、错误提示、播放令牌、修改密码和频道编辑表单，并通过 Release 构建与 JavaScript 语法检查。

---

## 🚀 v1.5.3 更新说明 (Release Notes)

### 🔐 管理端安全加固与长期运行稳定性修复

本版本根据真实 Docker 运行日志与 HLS 播放链路验收结果，修复了可能在 Docker 重启后导致服务异常退出、长期运行后填满 HLS 内存盘以及容器日志高速膨胀的隐蔽问题，并为 Web 控制台和管理 API 增加完整登录保护。

#### 核心变更

1. **🔐 新增管理端登录与 API 会话认证**
   - 管理员密码使用 `PBKDF2-SHA256` 加盐哈希保存，配置文件中不落明文；
   - 登录成功后使用随机 `HttpOnly + SameSite=Strict` 会话 Cookie，默认有效期 12 小时；
   - 所有管理 `/api/*` 接口统一鉴权并校验管理请求头，增加 5 次失败锁定 10 分钟的登录限速；
   - 新增退出登录与在线修改密码功能，修改密码后立即撤销其它旧会话；
   - 管理会话与播放鉴权完全解耦；`/api/health`、`/api/ready` 保持公开，Jellyfin 不依赖网页登录 Cookie；
   - Linux 每次保存配置后自动将 `config.json` 收紧为 `0600`，避免 Cookie、哈希和密文被其他本机用户读取。

2. **🔑 新增独立的 Jellyfin / IPTV 播放令牌**
   - 首次初始化生成 256 位 CSPRNG 随机令牌，订阅地址改为 `/p/{token}/jellyfin.m3u`，M3U 内的 HLS 清单和相对分片路径自动继承令牌；
   - 播放令牌不依赖 12 小时管理会话，网页登录退出、改密或会话过期不会中断 Jellyfin 拉流；
   - 服务端路由只用 SHA-256 摘要做固定时间校验；为便于登录后再次复制地址，令牌原文仅以管理员密码派生密钥进行 AES-256-GCM 加密保存；
   - 管理端可一键轮换令牌，旧 M3U/HLS 地址立即失效；可猜测的旧 `/jellyfin.m3u`、`/live/*` 和 `/stream/*` 不再暴露媒体；
   - 虎牙内部签名代理限制为容器回环访问，堵住绕过播放令牌直接读取上游清单的旁路。

3. **🛡️ 管理接口不再回传 Cookie 原文**
   - `/api/config` 改用安全 DTO，不再返回 `CookieProfiles` 原值或频道运行时 `Cookies` 副本；
   - Web 控制台只显示“已配置/未配置”，替换 Cookie 时需要重新粘贴，避免浏览器、扩展或网络调试记录泄露凭据；
   - 保存 Cookie 的响应也不再回显刚提交的 Cookie。

4. **🐳 修复 Docker 非交互启动竞态崩溃**
   - 仅在真实交互式终端中监听回车退出；Docker 的 `/dev/null` 不再触发错误的 `StopAsync()`；
   - 消除 Docker Engine 重启后 Kestrel 关闭与启动竞争引发的 `ObjectDisposedException` / 退出码 139 风险；
   - 容器继续使用标准 `SIGTERM` 完成优雅停止。

5. **🧹 根治 FFmpeg 日志填满 512 MiB HLS tmpfs**
   - FFmpeg 改用 `-hide_banner -nostats -loglevel warning`，停止每秒写入进度信息；
   - 每次会话重建日志文件，单频道日志硬限制为 2 MiB，达到上限后继续排空管道但丢弃额外日志；
   - FFmpeg 改用结构化参数列表，并正确发送 HTTP 头结尾 CRLF，消除启动警告与参数二次解析风险；
   - 避免日志与 HLS 切片争抢内存盘并在运行数天后造成断流。

6. **📉 停止容器控制台每秒整屏刷写**
   - 检测到重定向输出时自动关闭终端仪表盘重绘，仅保留关键生命周期与故障日志；
   - 下调 ASP.NET Core 逐请求日志噪声，防止 Docker `json-file` 日志高速轮转；
   - Windows 本地交互式终端仍保留原有实时仪表盘。

7. **📊 修复指标、健康检查与离线探测语义**
   - 状态未变化时不再刷新 `LastStateChange`，卡死判断可以得到真实状态持续时间；
   - 主播正常未开播不再累加 `ErrorCount`，改为独立的探测/离线探测计数；
   - AlwaysOn 离线或错误频道限制为每 30 秒探测一次，减少上游请求风暴；
   - `/api/health` 不再固定返回假 `healthy`：FFmpeg 缺失、明确错误或状态卡死时返回 HTTP 503，并新增 `/api/ready`；
   - FFmpeg 意外退出、清单陈旧、启动超时和手动重启均写入带频道、原因、退出码或时间差的生命周期审计日志。

---

## 🚀 v1.5.2 更新说明 (Release Notes)

### 📺 修复断流、卡顿及重启后片段重复播放

本版本针对 **Jellyfin 与 Web 管理面板试播时的断流、卡顿及片段重复播放** 进行直播链路专项修复，并增强按需推流与上游签名失效后的自动恢复能力。

#### 核心变更

1. **⏱️ HLS 时间轴在进程重启后保持单调递增**
   - FFmpeg 使用 Unix 时间作为媒体序号起点，并使用不复用的分片文件名；
   - 每次新时间轴写入 `DISCONTINUITY` 和节目时间戳，避免播放器把新分片误认成旧缓存；
   - 对外播放列表和 TS 分片统一禁止缓存，未被当前清单引用的旧分片在宽限期后清理。

2. **🐯 修复虎牙播放列表域名重复拼接与无限旧缓存回退**
   - 每次签名刷新都从新的 Master Playlist 按标准 URI 规则解析子列表；
   - 重写相对分片、密钥和初始化片段 URI；过期清单最多仅作 6 秒短暂容错。

3. **▶️ 修复 OnDemand 停流后无法再次拉起**
   - 停止 FFmpeg 时立即撤下旧播放列表；首次请求同时检查真实会话和清单新鲜度；
   - 同频道启动和手动重启通过锁串行化，避免并发启动多个 FFmpeg。

4. **🖥️ 增强管理面板试播恢复能力**
   - 使用更稳健的常规直播缓冲；网络错误自动重连，媒体错误自动恢复；
   - 播放长时间无进度或明显落后直播点时自动追赶，关闭窗口后完整释放恢复定时器。

5. **🌐 修复新安装的 M3U 主机地址模板**
   - `config.example.json` 的 `CustomHost` 默认留空，不再把说明文字误当成主机名；首次启动可按实际请求地址自动生成 Jellyfin 播放链接。

6. **🔒 发布包不再携带私有运行配置**
   - `dotnet publish` 明确排除真实 `config.json` 和旧 `dist` 目录，避免 Cookie/令牌进入发布包或递归夹带历史产物。

---

## 🚀 v1.5.1 更新说明 (Release Notes)

### 🛡️ 状态机重构、启动超时自愈与 Web 控制台体验升级

本版本针对 **FFmpeg 启动死锁卡在“启动中...”**、**频道状态机体系** 以及 **Web 控制台交互** 进行了多项重要修复与优化：

#### 核心变更

1. **🔄 FFmpeg 启动超时检测与僵死状态自愈**
   - 修复在 `AlwaysOn` 常驻推流模式下，因上游网络波动或源失效导致 FFmpeg 未生成 `stream.m3u8` 时无限死锁在“启动中...”的问题；
   - 为推流会话新增精准创建时间戳（`StreamingSession.CreatedAtUtc`），当启动超过 `StartupTimeoutSeconds`（默认 30 秒）仍未产出切片时，主动熔断终止卡死进程，探测上游状态并自动转为“未开播”或触发重试；
   - 增加无会话残留状态（`Starting`/`Restarting`）的 30 秒自动解除与重新拉起保护。

2. **📊 频道状态机规范化与轻量探测**
   - 全面梳理并严格规范状态分类：**推流中**（🟢 绿色动态声波）、**已开播**（🔵 青色常亮待机）、**未开播**（🟡 黄色常亮）、**启动中...**（🔵 蓝色脉冲）、**已停用**（⚫ 灰色）、**Cookie失效 / 异常**（🔴 红色）；
   - 在待机状态下引入每 30 秒一次的极轻量房间元数据探针（0 媒体流量消耗），精准感知主播开播与下播；
   - 彻底修复系统启动初期的“正在初始化...”残留问题。

3. **🖥️ Web 管理控制台体验优化**
   - 控制台主标题 `LiveStreamGateway` 新增直达 GitHub 开源仓库的超链接（新标签页打开）；
   - 移除页面冗余的底部 Footer 栏；
   - 顶部版本号调整为专业暗色等宽字体，与主标题保持同行展示；
   - 优化副标题的独占换行排版与响应式视觉；
   - 引入静态资源版本号（`?v=1.5.1`），彻底避免浏览器强缓存导致的新版样式失效。

4. **🐳 部署说明与 Windows Docker 警告**
   - 配置文件 `StreamingMode` 默认为 `AlwaysOn`（全天候秒开），同时完整支持 `OnDemand`（按需推流）；
   - 在 `README.md` 中新增针对 Windows Docker Desktop / WSL2 环境网络转发内存泄漏的部署警告与推荐运行方式。

---

## 🚀 v1.5.0 更新说明 (Release Notes)

### 🛡️ Docker 崩溃根治：按需推流模式 + 资源生命周期全面修复

本版本针对 **Docker Desktop 持续内存泄漏导致容器崩溃** 的问题进行了架构级修复。

#### 核心变更

1. **🆕 新增按需推流模式 (OnDemand)**
   - 支持 `StreamingMode` 切换（`"AlwaysOn"` 默认常驻推流 / `"OnDemand"` 按需推流）；
   - 在 `OnDemand` 模式下无人观看时 FFmpeg 自动停止（默认空闲超时 300 秒），彻底消除持续媒体流量触发 Docker Desktop 网络后端内存泄漏的根因；
   - 客户端访问 `/live/{channelId}/stream.m3u8` 自动触发 FFmpeg 启动，启动期间返回 `503 + Retry-After`，播放器会在 5 秒后自动重试；
   - 并发首次请求使用 per-channel 启动锁，确保同一频道只创建一个 FFmpeg（防启动风暴）；
   - 新增配置字段：`StreamingMode`（默认 `AlwaysOn`）、`IdleTimeoutSeconds`（默认 300）、`StartupTimeoutSeconds`（默认 30）、`PrewarmEnabledChannels`（预热列表）；
   - **完全向后兼容**：旧 `config.json` 无需修改。

2. **🔧 FFmpeg 资源生命周期全面修复**
   - 引入 `StreamingSession`（`IAsyncDisposable`）封装 FFmpeg 进程、日志 `StreamWriter`、`SemaphoreSlim` 和日志任务，确保每次 FFmpeg 重启后所有资源可靠释放，彻底消除文件句柄泄漏；
   - FFmpeg 停止时调用 `Process.Kill(entireProcessTree: true)` + `await WaitForExitAsync()` 终止完整进程树，避免孤儿进程遗留；
   - 日志任务支持协作取消（`CancellationToken`），进程停止后任务可干净退出。

3. **🌐 HttpClient 资源优化**
   - 全局共享 `HttpClient` 改用配置了连接生命周期（10 分钟）和空闲超时（2 分钟）的 `SocketsHttpHandler`，避免长期运行时连接池资源耗尽。

4. **📊 新增接口**
   - `GET /api/metrics`：返回每频道运行指标（推流状态、m3u8 刷新次数、启动/重启/错误计数、最后客户端访问时间）；
   - `GET /api/health`：健康检查接口，可被 Docker healthcheck 和监控系统调用。

5. **🐳 Docker Compose 改进**
   - 新增 `logging` 日志轮转（单文件 50MB，最多保留 3 个）；
   - 新增 `healthcheck`（每 30 秒检查 `/api/health`，连续 3 次失败触发重启）；
   - 新增 `deploy.resources.limits.memory: 1G` 防止 OOM 无限占用。

---

## 🚀 v1.4.0 更新说明 (Release Notes)

### ⚡ 直播流中继全链路防卡顿与性能专项升级

本版本针对高码率直播中继过程中的**间歇性卡顿、播放停顿与死锁缓冲**进行了全链路深度重构与优化：

1. **🚫 m3u8 播放列表端点显式禁用缓存**
   - 解决客户端、Jellyfin 代理层及反向代理缓存 `.m3u8` 导致客户端播放完旧分片后陷入停顿的问题；
   - 在 `/live/{channelId}/stream.m3u8` 响应中统一注入 `Cache-Control: no-cache, no-store, must-revalidate`、`Pragma: no-cache` 和 `Expires: 0`。

2. **🌐 跨平台 Referer 动态自适应**
   - 彻底修复各平台 FFmpeg 进程全部硬编码 B 站 Referer 的问题；
   - 根据频道实际所属平台动态注入对应的官方 `Referer`（虎牙/斗鱼/B站），消除上游 CDN 防盗链检测导致的限速、连接重置与 403 异常。

3. **💾 Docker `tmpfs` 内存盘容量扩容至 512MB**
   - 将容器内 HLS 切片内存盘由 128MB 扩容至 **512MB**；
   - 完美支撑多路 1080P60 / 4K / 原画直播流并发切片，杜绝内存盘满溢导致的磁盘 I/O 写入失败。

4. **⏱️ HLS 滑动窗口与容错缓冲扩展**
   - 将切片参数调优为 `-hls_time 3 -hls_list_size 15 -hls_delete_threshold 10`；
   - 播放列表覆盖窗口从 ~30 秒扩展至 **~75 秒以上**，大幅增强播放器面对网络偶发抖动时的容错抗抖能力。

5. **⚡ 虎牙代理 Master Playlist 跳过与子列表复用**
   - 重构虎牙 HLS 代理签名重写管道，直接基于缓存的子播放列表路径与最新签名组装目标 URL；
   - 消除每次 m3u8 刷新时向虎牙 CDN 请求两次的公网双重 RTT 开销。

6. **🛡️ 频道重启平滑过渡保护**
   - 频道重启或重连时不再粗暴清空已生成的全部 `.ts` 切片，仅重置 m3u8 列表并交由 FFmpeg 滑动窗口自动淘汰旧分片，防止正在播放的客户端遭遇 404 错误。

7. **🚀 日志 I/O 异步批量写入引擎**
   - 使用常驻 `StreamWriter` + 异步信号量批量刷新日志，彻底替代逐行同步 `File.AppendAllText`，消除 FFmpeg 高频日志导致的磁盘锁竞争与 CPU 上下文开销。

8. **🔄 僵尸流自愈检测周期提速**
   - 健康巡检周期由 25s 缩短至 **10s**，陈旧流判定阈值由 90s 缩短至 **30s**，僵死进程自愈提速近 3 倍。
